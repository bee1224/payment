# RY 程式待辦與未完成項目

> 更新日期：2026-07-09
> 代收收款方式已縮減後，這份清單只保留目前仍需要追的項目。

## 代收

| 項目 | 類別 | 狀態 | 說明 |
|---|---|---|---|
| callback retry / outbox | 收款 | 未完成 | 商戶 callback 失敗時仍缺正式重試機制 |
| 渠道必填欄位矩陣 | 收款 | 待確認 | `bank_account`、`store_number` 等欄位目前為補充欄位，仍可再細化各渠道建議 |

## 已不再納入本期範圍

以下歷史項目因目前代收渠道已收斂，不再視為待辦：

- 收款 `expired` 帶值
- 收款 `qr_url` 帶值
- 收款 `alipay_qrcode` 帶值
- 收款 `rate` 帶值
- 支付寶相關渠道或回應欄位
