# 核心資料庫結構與帳務流程

> 文件定位：目前 `migrations/001_init.sql` 已建立的**收款**資料結構。現行資料庫沒有 `payout_orders`、`payout_transactions` 或 `payout_callbacks`；代付持久化仍是規劃草案。

## 關聯

```mermaid
erDiagram
  merchants ||--o{ orders : owns
  merchants ||--o{ merchant_balances : has
  merchants ||--o{ ledger_entries : records
  payment_providers ||--o{ payment_channels : provides
  payment_channels ||--o{ orders : routes
  orders ||--o{ provider_transactions : maps
  payment_providers ||--o{ provider_transactions : handles
  payment_providers ||--o{ provider_callbacks : sends
  orders ||--o{ provider_callbacks : receives
  provider_transactions ||--o{ provider_callbacks : traces
  provider_transactions ||--o{ ledger_entries : creates
```

## 核心表

| 表 | 用途 |
|---|---|
| `merchants` | 下游商戶 |
| `payment_providers` | 目前實際使用藍新代收；結構可容納其他 Provider |
| `payment_channels` | 信用卡、ATM、超商等通道 |
| `orders` | 本系統代收訂單 |
| `provider_transactions` | 三方交易映射 |
| `provider_callbacks` | 三方通知紀錄 |
| `merchant_balances` | 商戶餘額 |
| `ledger_entries` | 帳本流水 |

RY 代付目前採同步 HTTP 代理，不會寫入上述資料表。

## 訂單狀態

```mermaid
stateDiagram-v2
  [*] --> pending
  pending --> paid
  pending --> failed
  pending --> expired
  paid --> refunded
```

## 通知處理

```mermaid
flowchart LR
  A[收到 notify] --> B[寫入 provider_callbacks]
  B --> C[查 provider_transactions]
  C --> D[更新 orders]
  D --> E[寫入 ledger_entries]
  E --> F[更新 merchant_balances]
```

## 掉單追蹤

```mermaid
flowchart LR
  A[orders] --> B[provider_transactions]
  B --> C[provider_callbacks]
  A --> D[ledger_entries]
  D --> E[merchant_balances]
```
