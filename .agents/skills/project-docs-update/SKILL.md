---
name: project-docs-update
description: Update only documentation directly affected by a completed change in this payment-service repository, preserving existing structure and accurately reflecting code.
--- 

Confirm the completed implementation and changed files first. Identify only related API, database, architecture, security, deployment, environment-variable, business-flow, or README documentation. Do not read or rewrite all documentation by default; explain why before expanding scope.

Update only affected passages. Preserve existing format, headings, ordering, and layout. Do not modify unrelated content or delete information unless it is confirmed obsolete. Never describe unfinished work as complete; align every update with implemented code and current behavior.

If no update is needed, state why and make no changes. Otherwise report changed documents and the headings added or changed. Never modify runtime code, secrets, or deployment state.
